@using Microsoft.AspNetCore.Components.Authorization
@inject NavigationManager Nav
@inject JwtAuthStateProvider AuthProvider

<header class="em-header">
    <div class="em-header-inner">
        <a class="em-logo" href="/">🎟 EventMarketplace</a>
        <nav class="em-nav">
            <a href="/events">Etkinlikler</a>
            <AuthorizeView>
                <Authorized Context="authCtx">
                    <AuthorizeView Roles="Admin">
                        <a href="/admin/dashboard" class="em-nav-admin">Admin Panel</a>
                    </AuthorizeView>
                    <button @onclick="HandleLogout" class="em-btn-link">Çıkış (@authCtx.User.Identity?.Name)</button>
                </Authorized>
                <NotAuthorized>
                    <a href="/login">Giriş Yap</a>
                    <a href="/register" class="em-btn-primary">Kayıt Ol</a>
                </NotAuthorized>
            </AuthorizeView>
        </nav>
    </div>
</header>

@code {
    private async Task HandleLogout()
    {
        await AuthProvider.LogoutAsync();
        Nav.NavigateTo("/login", forceLoad: false);
    }
}
